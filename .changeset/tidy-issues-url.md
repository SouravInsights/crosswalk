---
"@webmcp-stack/codegen": minor
---

Rename the package to `@webmcp-stack/codegen`, published under the webmcp-stack npm org. Same CLI, same features; the unscoped `webmcp-codegen` package is deprecated. The zero-install command is now `npx @webmcp-stack/codegen generate`. Also points the error-report URL at the renamed repo.
